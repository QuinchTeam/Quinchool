; @hookform/resolvers' optional peers conflict (@typeschema/valibot wants
; valibot ^0.39, resolvers wants ^1) and npm>=7 hard-fails on it even though
; valibot is unused here. Use npm6 peer behavior; our real peers are explicit.
legacy-peer-deps=true
